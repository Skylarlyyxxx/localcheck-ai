# Optional: enables the server-side Gemini semantic review. Never commit a real key.
GEMINI_API_KEY=
# Optional: change only if the default model is unavailable for your account.
GEMINI_MODEL=gemini-2.5-flash-lite
